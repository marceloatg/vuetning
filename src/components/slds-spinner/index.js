import sldsSpinner from './slds-spinner'
import sldsSpinnerContainer from './slds-spinner-container'

export default Vue => {
    Vue.component(sldsSpinner.name, sldsSpinner)
    Vue.component(sldsSpinnerContainer.name, sldsSpinnerContainer)
}
