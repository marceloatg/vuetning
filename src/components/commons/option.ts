/**
 * A base option used by component that allow selection of values based on labels.
 */
export interface Option {
    /**
     * Option label.
     */
    label: string


    /**
     * Option value.
     */
    value: string
}
