/**
 * A row rendered by `slds-data-table`. Rows are keyed by the column `fieldName`s
 * and otherwise free-form, so callers can attach any additional metadata.
 */
export type DataTableRow = Record<string, any>
