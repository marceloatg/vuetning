import sldsPill from './slds-pill.vue'
import sldsPillContainer from './slds-pill-container'

export default Vue => {
    Vue.component(sldsPill.name, sldsPill)
    Vue.component(sldsPillContainer.name, sldsPillContainer)
}
