/**
 * Side-effect entry that imports the full Vuetning stylesheet bundle:
 * the Salesforce Lightning Design System CSS, animation keyframes,
 * placeholder selectors and the Salesforce Sans font face declarations.
 *
 * Consumers can use this when they want all base styles without picking
 * individual component CSS files:
 *
 *   import "vuetning/styles"
 */
import "./assets/styles/main.scss"
